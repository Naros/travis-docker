language: ruby
rvm:
  - 2.3
services:
  - docker
before_install:
  - docker pull antora/antora
  - pwd
  - ls -la
  - docker run --rm --privileged -v $(pwd):/site -w="/site" antora/antora playbook.yml
  - docker ps -a
  - pwd
  - ls -la
  - ls -la ./_site
  - la -la ./_site/documentation
